#include <common/Imperium.h>

/*
namespace Imperium {
    namespace util {
        namespace mem {
            PVOID alloc( ULONG size ) {
                STARDUST_INSTANCE
                return Self->Win32.RtlAllocateHeap( NtProcessHeap(), HEAP_ZERO_MEMORY, size );
            }

            VOID free( PVOID ptr ) {
                STARDUST_INSTANCE
                Self->Win32.RtlFreeHeap( NtProcessHeap(), 0, ptr );
            }
        }
    }
}
*/
